import { AlunoDao } from './AlunoDao';

const object = {
    nome: 'leo',
    curso: '1',
    turma: '2',
    turno: '3',
    nascimento: '4'
}


var aluno = new Aluno();
aluno.aluno(object);
console.log(aluno.aluno);